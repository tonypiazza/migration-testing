apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: {{ .Values.clusterName }}
spec:
  version: {{ .Values.version }}
  http:
    {{- if .Values.http.psc.dnsName }}
    tls:
      selfSignedCertificate:
        subjectAltNames:
        - dns: {{ .Values.http.psc.dnsName | quote }}
    {{- end }}
    service:
      metadata:
        annotations:
          {{- if .Values.http.psc.enabled }}
          networking.gke.io/load-balancer-type: "Internal"
          networking.gke.io/internal-load-balancer-subnet: {{ .Values.http.psc.subnet | quote }}
          {{- end }}
      spec:
        type: {{ .Values.http.type }}
        {{- if not .Values.http.psc.enabled }}
        loadBalancerSourceRanges:
        {{- range .Values.http.allowedCIDRs }}
          - {{ . | quote }}
        {{- end }}
        {{- end }}
  nodeSets:
  {{- range .Values.nodeSets }}
  - name: {{ .name }}
    count: {{ .count }}
    config:
      node.store.allow_mmap: false
    podTemplate:
      spec:
        {{- if $.Values.installGcsPlugin }}
        # repository-gcs is bundled in Elasticsearch 8.0+ but must be installed
        # explicitly on 7.x. Required for GCS snapshot repositories on ES < 8.0.
        initContainers:
        - name: install-gcs-plugin
          command:
          - sh
          - -c
          - |
            bin/elasticsearch-plugin remove --purge repository-gcs || true
            bin/elasticsearch-plugin install --batch repository-gcs
        {{- end }}
        containers:
        - name: elasticsearch
          env:
          - name: ES_JAVA_OPTS
            value: {{ .javaOpts | quote }}
          resources:
            requests:
              memory: {{ .resources.requests.memory }}
              cpu: {{ .resources.requests.cpu | quote }}
            limits:
              memory: {{ .resources.limits.memory }}
              cpu: {{ .resources.limits.cpu | quote }}
    volumeClaimTemplates:
    - metadata:
        name: elasticsearch-data
      spec:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: {{ .storage }}
  {{- end }}
{{- if .Values.http.psc.enabled }}
---
apiVersion: networking.gke.io/v1
kind: ServiceAttachment
metadata:
  name: {{ .Values.clusterName }}-psc
spec:
  connectionPreference: ACCEPT_MANUAL
  natSubnets:
    - {{ .Values.http.psc.natSubnet }}
  proxyProtocol: false
  resourceRef:
    kind: Service
    name: {{ .Values.clusterName }}-es-http
  consumerAllowList:
    {{- range .Values.http.psc.consumerProjectIds }}
    - project: {{ . }}
      connectionLimit: {{ $.Values.http.psc.connectionLimit }}
    {{- end }}
{{- end }}
